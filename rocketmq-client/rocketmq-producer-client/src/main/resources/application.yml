server:
  port: 9091

spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

learning:
  rocketmq:
    producer:
      producer-type: default
      name-server-addr: localhost:9876
      producer-group: ROCKETMQ_CLIENT_LEARNING
