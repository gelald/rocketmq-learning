server:
  port: 9092

spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

learning:
  rocketmq:
    consumer:
      proxy-addr: localhost:8081
      consumer-switch:
        order: false
        filter: false
        transaction: false
